# homepage

Personal portfolio site with a separate browser-only utilities page at `utilities/`.

The utilities page is static and runs entirely in the browser so it does not need any server-side compute.